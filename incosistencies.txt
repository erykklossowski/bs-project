Underlying price (should be aFRR-DOWN, not CEN)

README currently treats CEN as the aFRR-down price proxy (e.g., “Source Field: cen_cost … used as the underlying asset price”). That contradicts the refactor where the underlying is the aFRR-down capacity price (from PSE endpoint like afrr_d).
Fix: Make aFRR-down capacity price (afrr_d) the primary price series. Keep CEN/COR/CSDAC/SK only as predictors for μₜ.

Volumes (should be actual procured MW, not synthetic)

README documents synthetic volumes derived from price (“create_synthetic_volumes”). That’s no longer acceptable—your budget/cap and sanity checks must use procured aFRR-down MW from PSE (e.g., zmb_afrrd or “Wielkości mocy bilansujących…” file).
Fix: Remove synthetic volume generation from the pipeline docs; require real aFRR-down MW time series.

Budget construction (must be explicit price×MW×hours)

README mixes “KMB guardrail” language with synthetic volumes. The final spec is to compute a per-interval aFRR-down spend: price_afrrd × mw_procured_afrrd × Δh, and cap portfolio revenues to your fair share. Ensure the doc states this clearly and not via arbitrary KMB allocation.
Fix: Document interval-level budget and the portfolio cap formula; keep KMB/KRO only as optional envelope cross-checks.

Data acquisition narrative (Node energy-prices vs. aFRR endpoints)

README drives everything through a “energy-prices-downloader.js” and then maps CEN to aFRR-down. This fights the new design that fetches aFRR-down price + aFRR-down MW directly (paged API calls; or the XLSX downloads you already have).
Fix: Replace that part with the single bash curl + paging segment that pulls the right endpoints (afrr_d prices, afrr_down MW, optional CEN/COR/SK for predictors). Keep the legacy route as optional.

Predictor mapping language

README calls the predictor mapping “available predictors = {cen_cost: CEN, …}” and then implies CEN is the underlying for bs_call_lognormal. That’s backwards with the refactor—CEN is not the underlying; it’s one of the Xₜ predictors into μₜ.
Fix: Clarify in text + code comments: predictors feed μₜ; underlying is price_pln_per_mw_h = aFRR-down.

Granularity & units wording

README says 15-minute alignment, but the earlier “per-interval unit” conversion is only implicit. Make the unit policy explicit: normalize all prices to PLN/MW-h; if file is per-interval, divide by Δh; expand hourly series across four quarters.
Fix: Add a one-paragraph “Units & cadence” box and a code comment pointer.

Code consistency checklist for black-scholes_refactored.py

Assuming your script is the refactored one we discussed, please ensure these are true in code:

 Price source: the column feeding bs_call_lognormal is aFRR-down price normalized to price_pln_per_mw_h (not CEN).

 Volumes: loaded from your aFRR-down MW series; no synthetic volume function is called anywhere.

 Budget per interval: budget_pln_interval = price_pln_per_mw_h * mw_procured * Hours_in_interval.

 Cap logic: portfolio PLN per interval is scaled to cap_share = budget_pln_interval * min(1, portfolio_mw / mw_procured).

 Predictors: optional; used only to build μₜ (ridge on log(price)); if absent, model falls back to intercept-only.

 Alignment: everything is re-indexed to a 15-min UTC grid; hourly inputs are step-held; price unit conversion respects Δh.

 Ex-post path: realized payoff path exists and is exported if --mode expost or both.

 Outputs: {prefix}_intervals.csv includes at least dt, price_pln_per_mw_h, mw_procured, budget_pln_interval, ...capped columns….